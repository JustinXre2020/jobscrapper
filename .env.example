# Gmail SMTP Configuration
# Get App Password from: https://myaccount.google.com/apppasswords
GMAIL_EMAIL=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Legacy single recipient (used if RECIPIENTS not set)
RECIPIENT_EMAIL=your_email@example.com

# Multi-recipient configuration (JSON format)
# Each recipient has: email, needs_sponsorship, search_terms
# Example:
# RECIPIENTS='[{"email": "user1@example.com", "needs_sponsorship": true, "search_terms": ["software engineer", "data analyst"]}, {"email": "user2@example.com", "needs_sponsorship": false, "search_terms": ["product manager"]}]'
RECIPIENTS='[{"email": "your_email@example.com", "needs_sponsorship": false, "search_terms": ["software engineer"]}]'

# Job Search Configuration (backup, used if RECIPIENTS not set)
SEARCH_TERMS=software engineer,product manager,data analyst
LOCATIONS=USA
# Default results per term (used when term not in RESULTS_WANTED_MAP)
RESULTS_WANTED=10
# Per-term scrape counts (JSON: term -> count)
RESULTS_WANTED_MAP='{"product manager": 100, "data scientist": 100, "data engineer": 50, "business analyst/data analyst": 50}'
HOURS_OLD=24

# Sites to scrape (linkedin, indeed, glassdoor, zip_recruiter)
SITES=indeed

# Redis Cache for Deduplication (optional)
REDIS_HOST=your-redis-ip-addr
REDIS_PORT=6379

# LLM Filter Configuration (OpenRouter API)
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Use :free suffix for free tier (rate limited to 20 req/min)
OPENROUTER_MODEL=liquid/lfm-2.5-1.2b-instruct:free
# Agent Workflow Configuration
# Max jobs processed simultaneously per batch in agent workflow
AGENT_CONCURRENCY=50
